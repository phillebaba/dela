
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.2.4
  creationTimestamp: null
  name: sharerequests.share.phillebaba.io
spec:
  group: share.phillebaba.io
  names:
    kind: ShareRequest
    listKind: ShareRequestList
    plural: sharerequests
    singular: sharerequest
  scope: Namespaced
  validation:
    openAPIV3Schema:
      description: ShareRequest is the Schema for the sharerequests API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: ShareRequestSpec defines the desired state of ShareRequest
          properties:
            appendHash:
              description: If hash should be appened to end of secret
              type: boolean
            intentRef:
              description: Refernce to intent to copy Secret from
              properties:
                name:
                  type: string
                namespace:
                  type: string
              required:
              - name
              - namespace
              type: object
            updateOnChange:
              description: If secret should be updated on change
              type: boolean
          required:
          - intentRef
          type: object
        status:
          description: ShareRequestStatus defines the observed state of ShareRequest
          type: object
      type: object
  version: v1alpha1
  versions:
  - name: v1alpha1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
